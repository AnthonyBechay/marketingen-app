# Use a flat (npm-style) node_modules layout instead of pnpm's symlinked one.
# Required so the Docker multi-stage COPYs of Prisma CLI/client work cleanly
# without dragging in the entire `.pnpm/` virtual store.
node-linker=hoisted
